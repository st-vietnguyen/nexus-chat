export const TABLES = {
  ROOMS: 'rooms',
  ROOM_MEMBERS: 'room_members',
  MESSAGES: 'messages',
  PROFILES: 'profiles',
} as const;

export const STORAGE_BUCKETS = {
  AVATARS: 'avatars',
} as const;
